server :
  port : 8880
spring :
  datasource :
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : # url
    username : # username
    password : # passwora
  jpa :
    database : mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    show-sql: true